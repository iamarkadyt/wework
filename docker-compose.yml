version: "3.1"
services:

  api:
    build: server
    restart: always
    env_file:
      - .env
    ports: 
      - ${PORT}:5000
    depends_on:
      - db

  db:
    build: db
    restart: always
    env_file:
      - .env
